# Research Copilot — direct dependencies (pinned to installed versions).
# Transitive deps (e.g. transformers, huggingface-hub) are pulled in automatically.

# --- Agent / orchestration ---
langgraph==1.2.4
langchain==1.3.4
langchain-core==1.4.0

# --- LLM provider (Gemini) ---
langchain-google-genai==4.2.4

# --- Document ingestion (PDF load + chunking) ---
langchain-community==0.4.2
langchain-text-splitters==1.1.2
pypdf==6.12.2

# --- Embeddings (local HuggingFace + torch) ---
langchain-huggingface==1.2.2
sentence-transformers==4.1.0
torch==2.12.0

# --- Vector store (Chroma) ---
langchain-chroma==1.1.0
chromadb==1.5.9

# --- External tools / config ---
arxiv==4.0.0
python-dotenv==1.2.2
